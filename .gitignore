# Environment and local secrets
.env
.env.*
!.env.example
!.env.sample

# Editor and OS files
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# Temporary files
tmp/
temp/
*.tmp
*.log
# dbt build artefacts — written inside the bind-mounted project by design
# (adr/0003), regenerated on every run, never reviewed.
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml

# Python bytecode — written into bind-mounted directories by the containers
__pycache__/
*.py[cod]
